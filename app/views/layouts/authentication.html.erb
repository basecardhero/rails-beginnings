<% content_for :content do %>
  <div class="min-h-screen flex flex-col sm:justify-center items-center pt-6 sm:pt-0 bg-gray-100">
    <div class="w-full sm:max-w-md">
      <h1 class="text-3xl text-center text-gray-600 hover:text-gray-800 mb-6 transition ease-in-out duration-150">
        <%= link_to "Rails Beginnings", root_path %>
      </h1>

      <%= render "partials/flash" %>

      <div class="bg-white px-8 py-7 overflow-hidden shadow rounded-md">
        <%= yield %>
      </div>
    </div>
  </div>
<% end %>

<%= render template: "layouts/application" %>
